const React = require('react');
const Layout = require('../Layout');

module.exports = function NewChannel({title}){
    <Layout title='Create new channel'>
        <div className='new-channel'>
            <h2>Create new channel</h2>
            <form method='POST' action=/>
        </div>
    </Layout>
}
